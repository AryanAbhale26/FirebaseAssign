specVersion: "v1beta"
serviceId: "assign"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "assign-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
